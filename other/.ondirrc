enter ~/projects/([^/]+)
    if [ -e .venv ]; then
        env=`cat .venv`
        if [ "$env" != "${VIRTUAL_ENV##*/}" ]; then
            workon $env
        fi
    fi

leave ~/projects/([^/]+)
    deactivate > /dev/null 2>&1
